{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "pnpm run build",
  "crons": [
    {
      "path": "/api/events",
      "schedule": "*/5 * * * *"
    }
  ]
}
